
<nav class="navbar mb-2 shadow-lg bg-neutral">
	<div class="container mx-auto">
		<div class="flex-none px-2 mx-2">
			<h1 class="text-3xl text-white">{{title}}</h1>
		</div>
	</div>

	<div class="flex-1 px-2 mx-2">
		<div class="flex justify-end">
			<a href="{{ path_for('menu.home') }}" class="btn btn-primary btn-sm rounded-btn ml-1">Main Menu</a>
			<a href="{{ path_for('menu.home') }}" class="btn btn-primary btn-sm rounded-btn ml-1">LogOut</a>
		</div>
	</div>
</nav>